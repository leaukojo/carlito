# TODO

Open work lives as one prompt per file in `docs/plans/` (effort + mode in each header; a
finished plan is deleted, its conclusion distilled into the nearest `CLAUDE.md`). Accepted
constraints that are not work are recorded in the `CLAUDE.md` beside the code they bind.
