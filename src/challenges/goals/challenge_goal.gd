class_name ChallengeGoal
extends ChallengeCheck
## One step of a def's ORDERED goal list: only the current goal is stepped, and it returns RUNNING
## until it passes or fails. A goal never returns RESET.
